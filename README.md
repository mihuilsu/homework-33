# 🚀 Gulp Automation Project

Проєкт демонструє автоматизацію веб-розробки за допомогою Gulp, включаючи компіляцію SCSS, автоматичне оновлення браузера, додавання вендорних префіксів та мінімізацію CSS.

## 📋 Зміст

- [Особливості](#особливості)
- [Технології](#технології)
- [Встановлення](#встановлення)
- [Використання](#використання)
- [Структура проєкту](#структура-проєкту)
- [Налаштування Gulp](#налаштування-gulp)
- [Деплой на GitHub Pages](#деплой-на-github-pages)

## ✨ Особливості

### 1. 📝 Компіляція SCSS
- Автоматична компіляція файлів SCSS у CSS
- Підтримка вкладених правил, змінних та міксінів
- Створення як розширеної, так і мінімізованої версії CSS

### 2. 🔄 Live Reload
- Автоматичне оновлення браузера при зміні файлів
- Синхронізація скролінгу між пристроями
- Локальний сервер на порту 3000

### 3. 🔧 Вендорні префікси
- Автоматичне додавання префіксів за допомогою Autoprefixer
- Підтримка останніх 2 версій браузерів
- Покращена кросбраузерна сумісність

### 4. ✨ Форматування коду
- Expanded стиль для розробки
- Мінімізована версія для продакшену
- Source maps для зручного дебагінгу

### 5. ⚡ Оптимізація
- Мінімізація CSS файлів
- Видалення коментарів та зайвих пробілів
- Зменшення розміру файлів на 30-50%

## 🛠️ Технології

- **Gulp** 4.0.2 - інструмент автоматизації
- **Sass** 1.69.5 - CSS препроцесор
- **Autoprefixer** 8.0.0 - додавання вендорних префіксів
- **CleanCSS** 4.3.0 - мінімізація CSS
- **Browser-sync** 2.29.3 - live reload сервер
- **Del** 6.1.1 - видалення файлів

## 📥 Встановлення

### Передумови
- Node.js (версія 14 або новіша)
- npm або yarn

### Крок 1: Клонування репозиторію

```bash
git clone https://github.com/mihuilsu/homework-37.git
cd homework-37
```

### Крок 2: Встановлення залежностей

```bash
npm install
```

або якщо використовуєте yarn:

```bash
yarn install
```

## 🚀 Використання

### Режим розробки

Запускає локальний сервер з live reload:

```bash
npm run dev
```

Сервер буде доступний за адресою: `http://localhost:3000`

Що відбувається:
- Компілюються SCSS файли
- Копіюються HTML та JS файли
- Запускається Browser-sync
- Відстежуються зміни у файлах

### Збірка для продакшену

Створює оптимізовану версію проєкту:

```bash
npm run build
```

Результат буде в папці `dist/`:
- `main.css` - розширена версія
- `main.min.css` - мінімізована версія

### Очищення

Видаляє папку dist:

```bash
npm run clean
```

## 📁 Структура проєкту

```
gulp-automation-project/
│
├── src/                      # Вихідні файли
│   ├── scss/                 # SCSS стилі
│   │   ├── main.scss        # Головний файл стилів
│   │   ├── _variables.scss  # Змінні (кольори, шрифти)
│   │   ├── _mixins.scss     # Міксіни (функції SCSS)
│   │   └── _components.scss # Компоненти (картки, кнопки)
│   ├── js/                  # JavaScript файли
│   │   └── main.js          # Головний JS файл
│   └── index.html           # HTML сторінка
│
├── dist/                    # Згенеровані файли (після збірки)
│   ├── css/
│   │   ├── main.css
│   │   └── main.min.css
│   ├── js/
│   │   └── main.js
│   └── index.html
│
├── .github/                 # GitHub Actions
│   └── workflows/
│       └── deploy.yml       # Автоматичний деплой
│
├── node_modules/            # Залежності (не в git)
├── .gitignore              # Ігноровані файли
├── gulpfile.js             # Конфігурація Gulp
├── package.json            # Залежності та скрипти
└── README.md              # Документація
```

## ⚙️ Налаштування Gulp

### Основні завдання (tasks)

#### `clean`
Видаляє папку dist перед новою збіркою:

```javascript
function clean() {
  return del(['dist']);
}
```

#### `styles`
Компілює SCSS в CSS:
1. Читає всі `.scss` файли
2. Компілює в CSS (expanded стиль)
3. Додає вендорні префікси
4. Зберігає `main.css`
5. Мінімізує CSS
6. Зберігає `main.min.css`
7. Оновлює браузер

#### `html`
Копіює HTML файли з `src/` до `dist/`

#### `scripts`
Копіює JavaScript файли з `src/js/` до `dist/js/`

#### `serve`
Запускає локальний сервер та відстежує зміни:
- Сервер на порту 3000
- Відстеження змін у SCSS, HTML та JS
- Автоматичне оновлення браузера

#### `build`
Повна збірка проєкту:
1. Очищення (`clean`)
2. Паралельне виконання (`styles`, `html`, `scripts`)

#### `dev`
Режим розробки:
1. Збірка проєкту
2. Запуск сервера з live reload

### Налаштування Autoprefixer

```javascript
.pipe(autoprefixer({
  overrideBrowserslist: ['last 2 versions'],
  cascade: false
}))
```

Додає префікси для останніх 2 версій браузерів.

### Налаштування мінімізації

```javascript
.pipe(cleanCSS({
  level: 2
}))
```

Рівень 2 включає:
- Видалення дублікатів
- Злиття правил
- Оптимізацію властивостей

## 📝 Додаткові можливості

### Додавання нових стилів

Створіть новий partial файл в `src/scss/`:

```scss
// src/scss/_buttons.scss
.my-button {
  background: $primary-color;
  padding: $spacing-medium;
}
```

Імпортуйте в `main.scss`:

```scss
@import 'buttons';
```

### Використання змінних

Всі змінні в `_variables.scss`:

```scss
$primary-color: #3498db;
$spacing-medium: 1rem;
```

Використання:

```scss
.element {
  color: $primary-color;
  padding: $spacing-medium;
}
```

### Використання міксінів

Міксіни в `_mixins.scss`:

```scss
.my-card {
  @include border-radius(8px);
  @include box-shadow($box-shadow-medium);
  @include transition(transform);
}
```

## 📄 Ліцензія

Цей проєкт ліцензовано під MIT License.

## 👤 Автор

**Ваше Ім'я**
- GitHub: [@mihuilsu](https://github.com/mihuilsu)

## 📞 Підтримка

Якщо у вас є питання або проблеми:
- Створіть [Issue](https://github.com/mihuilsu/homework-37/issues)